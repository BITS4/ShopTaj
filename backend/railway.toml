[build]
builder = "NIXPACKS"
buildCommand = "npm install --include=dev && npx prisma generate && ./node_modules/.bin/nest build"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
