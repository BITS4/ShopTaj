[build]
builder = "NIXPACKS"
buildCommand = "npm install && ./node_modules/.bin/nest build && npx prisma generate"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
